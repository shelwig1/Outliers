class work:
	#start, stop, notes, date
	#time, notes, 

	#we don't need multiple constructors - timer class can compute time, then pass it to this constructor - no problem
	def __init__(self, date, time, notes):
		self.date = date
		self.time = time
		self.notes = notes

	#edit any given value

	#linked list implementation
